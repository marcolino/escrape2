- where to put common code?
    function inArray(arr, obj) {
      return (arr.indexOf(obj) != -1);
    }
- user all lowercase for errors
- adopt grunt
- use grunt-remove-logging

V remove all "err.context = " lines