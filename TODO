- where to put common code?
    function inArray(arr, obj) {
      return (arr.indexOf(obj) != -1);
    }
- user all lowercase for errors
- adopt grunt
- use grunt-remove-logging
- add mongo dependency and (http://docs.mongodb.org/manual/installation/) start it in deploy script...
- remember to uninstall module 'socket.io', if not used for tailable cursor view...

export NODE_DEBUG=request
https://www.npmjs.com/package/request-replay

V remove all "err.context = " lines